# EXEC_PLAN｜VowPic 商业页面与收费链路最小修复

状态：ACTIVE
最后更新：2026-08-27 15:06 +08:00

## 1. 启动：任务合同

### 目标

- 在不更换既有技术与商业供应商的前提下，修复收费展示错误、首页商业信息结构、加载误报、功能承诺不一致和未登录购买体验，并以可验证的一次 PR 和最多一次生产发布恢复完整对外运营页面。

### 交付物

- 后端商品分类修复与自动化测试。
- 前端付款弹窗、首页、加载状态、登录回跳及必要测试。
- 后端测试、前端类型检查、单元测试、Web build、桌面与移动浏览器验收证据。
- 一个 PR、非生产 CI 结果、合并 SHA；满足门禁时最多一次 Production 及线上验收证据。

### 范围内

- 积分包与订阅商品接口和展示。
- 付款弹窗收费说明和内部代码隐藏。
- 首页八段最小商业结构：首屏、结果示例、三步流程、适用场景、定价、隐私安全、FAQ、最终 CTA、页脚。
- 首次加载骨架/明确错误态、现有功能承诺校准、未登录购买回跳。
- 与改动直接相关的测试、构建、浏览器验收、一个 PR、非生产 CI、最多一次 Production。

### 范围外

- App、小程序、新供应商、新复杂控制系统、伪造社会证明。
- 更换 Supabase、Creem、EvoLink、Vue/Uni-app、FastAPI 或生成模型。
- 未经再次明确授权的真实付款、订阅或退款。

### 必须保持不变

- 现有品牌视觉和完整商业能力方向。
- 积分规则：欢迎积分与各生成模式收费必须以服务端权威配置为准。
- 私有上传、生成结果和下载访问边界。
- 三小时硬上限、同一根因最多两轮、最多一次 Production。

### 完成标准

- 积分包接口只返回一次性积分包，订阅接口只返回订阅；页面不显示内部 SKU/retention 代码，金额与积分/周期清楚。
- 首页结构齐全、加载期间不误报 browse-only、承诺与真实能力一致、未登录购买可登录后回到原购买意图。
- 相关自动化验证全部通过，桌面和 390px 移动端无横向溢出且关键 CTA 可用。
- 非生产 CI 全绿并合并；若生产前置和回滚条件满足，只执行一次 Production，线上版本精确对应 merge SHA，并验证 Google 登录入口、商品分类、上传、生图、积分与私密下载。
- 未执行真实付费时明确标记该项未验证；只要授权范围内的真实线上验收全部通过，仍可完成本计划，但不得声称真实付款链路已验收。

### 本轮模式

- 制定并执行。

## 2. 规划：里程碑与任务

### 里程碑

1. 收费正确性：商品分类、名称、周期、积分成本和未登录购买路径。
2. 页面最小整理：首页结构、加载态、承诺一致性和响应式。
3. 本地验收：后端、前端、构建、桌面和移动浏览器。
4. 交付与发布：一个 PR、非生产 CI、合并、最多一次生产和线上验收。

### 任务清单

| ID | 任务 | 依赖 | 输入 | 输出 | 状态 | 完成证据 |
|---|---|---|---|---|---|---|
| T-01 | 锁定最新 main 基线与唯一执行计划 | 无 | origin/main、旧计划 | 新分支与本计划 | 已验证 | 分支 `codex/vowpic-commercial-ux` 基于 `066c3a6`；计划可重读 |
| T-02 | 修复商品分类与收费展示 | T-01 | catalog、API、PaymentModal | 后端/前端最小改动与测试 | 已验证 | 积分包接口仅返回 `pack_50/120/300`；定向后端与前端单测通过；浏览器显示人类可读名称和续费说明 |
| T-03 | 修复未登录购买和加载/承诺一致性 | T-02 | ops/auth/create/home | 最小交互与文案改动 | 已验证 | 登录回跳工具单测通过；首页/创建页/详情页加载期间不误报 browse-only；文案与肖像上传边界一致 |
| T-04 | 整理首页八段结构 | T-02 | 现有首页与真实服务端数据 | 响应式首页 | 已验证 | 浏览器断言段落顺序正确；桌面与 390px 移动端通过，无横向溢出 |
| T-05 | 完成本地全套相关验证 | T-02~T-04 | 修改后的代码 | 测试、类型检查、build 证据 | 已验证 | 后端 29 项、前端 38 项、类型检查、Web build、浏览器 2 项全部通过 |
| T-06 | 创建一个 PR 并通过非生产 CI | T-05 | 单一分支 | 单一 PR、全绿 CI | 处理中 | PR #238；首轮 CI 发现 OpenAPI 生成文件未同步，正在同一 PR 修复 |
| T-07 | 合并并执行最多一次 Production | T-06 | merge SHA、回滚条件 | 生产部署与线上证据 | 待处理 | deployment/version/E2E |

### 资产保存位置

- 正式产物：`backend/`、`frontend/` 及直接相关测试文件。
- 计划与恢复：项目根目录 `EXEC_PLAN.md`；规范副本 `.agent/PLANS.md`。
- 浏览器验收：使用本地临时截图，不提交仓库。

### 关键风险与阻塞

- 当前线上运行 SHA 落后于 main；生产必须部署精确 merge SHA，不能仅切换能力开关。
- Google 登录需要真实 Edge 用户会话确认；自动化只验证入口和回调边界，不能冒充用户账号。
- 生成会消耗真实积分/供应商额度；只使用已有测试账号和最小样例，不执行真实付费。
- 2 小时仍未完成代码与本地验证则缩减为收费与加载关键修复；2 小时 15 分仍未达到生产就绪则停在生产前。

## 3. 执行：当前工作

- 当前里程碑：4. 交付与发布。
- 当前任务：T-06 创建唯一 PR 并运行非生产 CI。
- 当前动作：同步后端 OpenAPI 与前端生成类型，验证确定性后更新同一 PR，并等待第二轮非生产 CI。
- 本轮实际产物：商品分类、用户文案、登录回跳、加载态、首页八段结构、后端/前端单元测试与本地浏览器验收代码。
- 下一项已授权任务：完成 T-06；仅在 CI 全绿、回滚条件明确后进入 T-07。

## 4. 监控与调整

### 实际进度

- 2026-08-27 14:23：读取复杂任务模板、UI/UX 与浏览器技能；确认旧工作分支落后于 `origin/main`，已从最新 main 创建本次分支。
- 2026-08-27 14:46：后端积分包按 `product_kind=credit_pack` 分流；前端移除内部 SKU/retention code，未登录购买保留套餐意图；首页改为结果、三步、场景、定价、隐私、FAQ、CTA；能力未加载时改为中性加载态。
- 2026-08-27 14:52：后端定向测试 29 项通过（另含 6 个子测试）；前端类型检查通过，11 个单测文件共 38 项通过，Web build 通过。
- 2026-08-27 14:56：本地 Edge 无头进程在加载页面前异常退出（code 21），未作为产品失败重复尝试；自动化改用仓库 Playwright Chromium。
- 2026-08-27 15:00：桌面和 390px 移动端浏览器验收共 2 项通过；同一 Uni-app 按钮内部节点定位达到两轮上限后，浏览器测试改为验证可见购买入口与条款，登录回跳参数由独立单元测试验证。
- 2026-08-27 15:03：创建唯一 PR #238，提交 `f8ba686`；PR 仅触发非生产 CI 和 Vercel Preview。
- 2026-08-27 15:05：首轮 CI 的 `Require deterministic committed OpenAPI` 失败，原因是响应模型新增 `product_kind` 后未同步提交生成文件；已从正式导出脚本生成 `openapi/openapi.json` 和 `frontend/src/generated/api.d.ts`，尚待本地确定性复核与推送。

### 新发现

- 发现：旧 `EXEC_PLAN.md` 对应已过期的生产上线任务且内容乱码。
- 证据：旧计划记录 source SHA `2ec3709`，当前 `origin/main` 为 `066c3a6`。
- 影响：更新同一份计划，不沿用旧坐标，不保留第二份活动计划。
- 发现：仓库 CI 要求任何响应模型变化同步提交 OpenAPI 和前端生成类型。
- 证据：PR #238 首轮 CI run `33048325913` 的后端任务在 `Require deterministic committed OpenAPI` 阶段失败；两个生成文件各新增 6 行 `product_kind` 合同。
- 影响：作为本次 API 修复的直接产物补充到同一 PR，不改变业务范围。

### 用户确认的变更

- 日期：2026-08-27。
- 最新要求：按十条最小处理方案制定并执行；一个 PR、非生产 CI、最多一次生产、真实付款另行授权。
- 对范围、顺序或完成标准的影响：先收费正确性，再页面整理，再本地/CI/生产验收。

### 当前阻塞

- 阻塞：无。
- 证据：本地全部约定门禁通过，当前处于提交前检查。
- 解除条件：不适用。

## 5. 收尾与验收

### 交付检查

- [x] 交付物真实存在
- [x] 后续要使用的资产已经保存
- [x] 文件可以重新读取、打开或运行
- [x] 必要本地测试和浏览器检查已完成
- [ ] 用户要求的效果已经实现
- [ ] 未验证项与风险已经说明

### 验证结果

- 验证方式：后端 pytest；前端 TypeScript 类型检查、Vitest、Uni-app Web build；Playwright Chromium 桌面和 390px 移动端。
- 实际结果：后端 29 项通过（另含 6 个子测试）；前端 38 项通过；类型检查和 Web build 退出码 0；浏览器 2 项通过。真实付款/订阅/退款未执行。
- 证据位置：仓库测试文件、命令退出码及 `frontend/dist/build/web` 构建产物；PR/CI 与生产证据待补充。

### 最终结果

- 已完成：T-01 至 T-05。
- 未完成：T-06 单 PR/非生产 CI；T-07 合并、最多一次 Production 及线上验收。
- 风险：生产与真实用户/供应商交互必须以真实证据为准。
- 用户验收：待完成。

### 恢复位置

- 从 T-06 提交前范围和工作流触发检查继续；不得重复本地 Edge 无头启动或 Uni-app 内部按钮节点定位。
