# 现有功能梳理（历史文档入口）

> 状态：历史归档说明  
> 说明：原始版本存在明显编码污染与过期实现描述，已不再作为当前项目权威文档。

---

## 当前应优先查看

1. `docs/PRD.md`
   - 当前产品范围、P0/P1/P2 目标、约束与验收口径。
2. `docs/实施任务清单_清洁版.md`
   - 当前研发推进状态、阶段完成度、剩余阻塞项。
3. `docs/商用闭环打通说明.md`
   - 商用闭环、部署前提、严格就绪检查与外部依赖说明。

---

## 为什么不再使用原始内容

- 原文存在编码污染，影响可读性与维护。
- 原文包含历史双链路描述，例如旧 `webhook` / `replicate` / 多入口兼容结构，和当前默认主链路不一致。
- 原文中的部分接口、部署口径、状态机说明已经被后续实现覆盖。

---

## 当前主链路结论

- 默认生成主链路：`Jiekou + ComfyUI`
- 默认支付主链路：`Creem`
- 当前权威后端入口与配置要求，应以 `docs/商用闭环打通说明.md` 为准。

---

## 如果需要恢复历史背景

- 请通过 Git 历史查看旧版本，而不是继续编辑污染文本。
- 若必须保留历史信息，建议重新整理为新的干净文档，再引用到当前体系中。
